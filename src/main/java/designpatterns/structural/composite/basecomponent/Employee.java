package designpatterns.structural.composite.basecomponent;

/**
 * 1) declares the interface component used to access and 
   manage child components(Developer and Manager)
   2) implements common default behavior
 */
public interface Employee {
	
	public void showEmployeeDetails();

}
